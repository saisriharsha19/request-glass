{
  "name": "return-radar",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun --watch server.ts",
    "check": "bunx tsc --noEmit",
    "test": "bun test",
    "test:browser": "playwright test",
    "build:cloudflare": "bun run scripts/build-cloudflare.ts",
    "dev:cloudflare": "wrangler dev",
    "deploy:cloudflare": "wrangler deploy",
    "build:pages": "bun run scripts/build-pages.ts",
    "deploy:pages": "bun run build:pages && wrangler pages deploy dist-cloudflare --project-name tuckday --branch main"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "@types/bun": "latest",
    "typescript": "latest",
    "wrangler": "^4.131.2"
  },
  "dependencies": {
    "@tesseract.js-data/eng": "^1.0.0",
    "fflate": "^0.8.3",
    "pdfjs-dist": "^6.3.289",
    "tesseract.js": "^7.0.0"
  }
}
